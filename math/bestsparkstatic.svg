<svg xmlns="http://www.w3.org/2000/svg" width="100vw" height="100vh" viewBox="0 0 1920 1080">
  <foreignObject width="100%" height="100%">
    <div xmlns="http://www.w3.org/1999/xhtml" style="padding:40px; box-sizing:border-box; height:100%; width:100%; background-color:#f5f5f5;">
      
      <button type="button" onclick="fetchAndInject(event)" style="padding:15px 30px; font-size:18px; font-weight:bold; cursor:pointer; background-color:#007bff; color:white; border:none; border-radius:5px;">
        Load Now &amp; idk 
      </button>
      
      <br/>
      <br/>
      
      <iframe id="target-iframe" sandbox="allow-scripts allow-same-origin allow-downloads allow-top-navigation-by-user-activation"
        
        style="width:100%; height:calc(100% - 100px); border:2px solid #333; background-color:white; border-radius:8px;"></iframe>
      
      <script type="text/javascript">
        async function fetchAndInject(e) {
          if (e) { e.preventDefault(); e.stopPropagation(); }
          
          const iframe = document.getElementById('target-iframe');
          const url = 'https://cdn.jsdelivr.net/gh/zaka13X/svgs/bestsparkstatic.html'; 
          
          try {
            const response = await fetch(url);
            if (!response.ok) throw new Error('HTTP ' + response.status);
            
            iframe.srcdoc = await response.text(); 
            
          } catch (err) {
            /* FIX: Changed less-than symbol to an escaped entity string so the XML parser doesn't break */
            iframe.srcdoc = '&lt;p style="color:red; font-size:18px; padding:20px;"&gt;Failed: ' + err.message + '&lt;/p&gt;';
          }
        }
      </script>

    </div>
  </foreignObject>
</svg>
